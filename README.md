# crucible-go

This is the Go module for the Crucible project.

## Structure

- `src/` — Main Go source code directory
    - `main.go` — Entry point for the application

## Usage

To run the main application:

```sh
cd src
go run main.go
```

To add dependencies, use:

```sh
go mod tidy
```
